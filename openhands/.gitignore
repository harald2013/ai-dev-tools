# Host-specific overrides (e.g. OPENHANDS_UID/GID) — never commit.
.env

# Entire .openhands/ tree is runtime state generated by the container
# (API/secret keys, conversation data, automation DB, workspaces) — verified
# by running the container; there is no committed config template inside it.
.openhands/
